version: "2.1"

services:
  discovery:
      volumes:
      - ${BITBUCKET_CLONE_DIR}/edx-themes:/edx/app/edx-themes:cached
  ecommerce:
      volumes:
      - ${BITBUCKET_CLONE_DIR}/edx-themes:/edx/app/edx-themes:cached
  lms:
      volumes:
      - ${BITBUCKET_CLONE_DIR}/edx-themes:/edx/app/edx-themes:cached
  studio:
      volumes:
      - ${BITBUCKET_CLONE_DIR}/edx-themes:/edx/app/edx-themes:cached
